<controls:FancyWindow xmlns="https://spacestation14.io"
                      xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
                      xmlns:customControls="clr-namespace:Content.Client.Administration.UI.CustomControls"
                      Title="{Loc 'surgery-menu-title'}"
                      SetSize="500 300"
                      MinSize="500 300">

  <BoxContainer Orientation="Horizontal" HorizontalExpand="true" VerticalExpand="true" Margin="6">
    
    <!-- Левая панель -->
    <BoxContainer Orientation="Vertical" HorizontalExpand="true" VerticalExpand="true" MinWidth="200">
      <Label Text="Области операций" 
             HorizontalAlignment="Center"
             Margin="0,0,0,6"/>
      
      <ScrollContainer VerticalExpand="true" HorizontalExpand="true">
        <BoxContainer Name="GroupListContainer" 
                     Orientation="Vertical"
                     HorizontalExpand="true"/>
      </ScrollContainer>
    </BoxContainer>

    <!-- Разделитель -->
    <customControls:VSeparator StyleClasses="LowDivider" Margin="0 0 0 10"/>
    
    <!-- Правая панель -->
    <BoxContainer Orientation="Vertical" HorizontalExpand="true" VerticalExpand="true">
      <Label Text="Шаги операции" 
             HorizontalAlignment="Center"
             Margin="0,0,0,6"/>
      
      <ScrollContainer VerticalExpand="true" HorizontalExpand="true">
        <BoxContainer Name="StepListContainer" 
                     Orientation="Vertical"
                     HorizontalExpand="true"/>
      </ScrollContainer>
    </BoxContainer>

  </BoxContainer>
</controls:FancyWindow>